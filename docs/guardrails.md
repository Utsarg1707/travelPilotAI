# Input & Output Guardrails Specification

## Overview
Documents the input guardrail node (prompt injection detection, scope validation, query normalization) and output guardrail node (schema verification, conflict checking, budget sanity enforcement).
