# Human-in-the-Loop (HITL) Specification

## Overview
Details the LangGraph interrupt and resume mechanics, checkpoint state persistence, approval states (`APPROVE`, `EDIT`, `REJECT`), and UI integration.
